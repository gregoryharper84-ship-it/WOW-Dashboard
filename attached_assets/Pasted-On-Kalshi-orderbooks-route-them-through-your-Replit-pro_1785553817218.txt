On Kalshi orderbooks: route them through your Replit proxy, not direct. Your proxy already does the RSA request-signing for order book pulls (that's stated in your own WOW infrastructure — Replit owns order book/fee math), so it's the existing single source of truth for that signing logic. Building a second direct-to-Kalshi orderbook action in the GPT would just be a path that 401s in production and gives you two places to maintain signing behavior if Kalshi ever changes it. The other 6 endpoints are genuinely public — direct is fine there.

On the paid vendors, that's information only you have — I don't have visibility into what accounts or keys you're carrying.Good — stub all of it, then. Two categories worth keeping distinct in the schemas so it's clear what "later" means for each:

Sportradar/SportsDataIO, PrizePicks board vendor, odds feed — these are real, existing paid vendors with real OpenAPI-documentable endpoints. Stub with the correct schema shape and a placeholder `api_key` field; when you sign up, it's just credential entry, no schema rework.

Identity-resolution, probability-model, calibration DB, news feed — these aren't vendor accounts, they're custom backend services that don't exist yet anywhere (not even unbuilt on a vendor's side). For these, stub with a placeholder `base_url` pointing at nothing, and flag in the action description that it's `NOT_YET_DEPLOYED` — same as your Kalshi weather lane's `DRY_RUN_ONLY` pattern. Worth deciding later whether these get built on your existing Replit app or a separate service, since that affects the schema (paths, auth type) more than just filling in a key would.

Summary to carry back into the GPT build: Kalshi orderbooks → proxy through Replit; the other 6 Kalshi endpoints → direct public, no auth; Sportradar/SportsDataIO/PrizePicks vendor/odds feed → stubbed with placeholder `api_key`; identity-resolution/probability-model/calibration-DB/news → stubbed with placeholder `base_url`, labeled not-yet-deployed.