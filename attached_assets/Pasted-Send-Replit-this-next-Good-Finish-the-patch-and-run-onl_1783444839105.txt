Send Replit this next:

Good. Finish the patch and run only the production-readiness verification now.

Do not add new features. Do not refactor unrelated files.

Run these checks:

1. Restart api-server + Flask backend cleanly.
2. Run TypeScript typecheck.
3. Run Python terminal bucket tests:
   `cd artifacts/flask-scoring-api && python -m pytest gate_engine/tests/test_terminal_buckets.py -v`
4. Run smoke test:
   `python artifacts/flask-scoring-api/scripts/smoke_test.py`
5. Run live normalize test:

   * Pull 5–10 live Odds API props.
   * Confirm source_status is `RETRIEVED`, not `LIVE`.
   * Confirm team_confidence appears on every prop.
   * Confirm no player team is guessed from event home/away.
6. Run score-batch test:

   * Score 5 live props.
   * Confirm Request Log count increases by 5.
   * Confirm each score_batch log row includes:
     run_source=score_batch,
     source_status,
     source_grade,
     team_confidence,
     terminal_label,
     blocker,
     can_execute=false,
     execution_rule=READ_ONLY_NO_EXECUTION.
7. Run mock/fallback test:

   * Confirm source_status=DATA_UNOBTAINABLE.
   * Confirm blocker priority includes DATA_UNOBTAINABLE and MOCK_FALLBACK_SOURCE before STATUS_NOT_CONFIRMED.
   * Confirm 0 mock rows reach MONEY_QUALIFIED or FINAL_APPROVED.
8. Run MCP safety test:

   * Confirm tools/list returns 13 allowed tools.
   * Confirm disallowed execution-style tool returns LIVE_EXECUTION_DISABLED.
   * Confirm audit log records tool call with no secrets.

Return only:

* Files changed
* Tests run
* Typecheck result
* Pytest result
* Smoke test result
* Live prop example
* Mock/fallback blocker example
* Score-batch Request Log count before/after
* Bucket distribution for score-batch
* MCP safety result
* Final confirmation:

  * no LIVE source_status exposed
  * no bare REJECT terminal labels exposed
  * no LOW team_confidence prop can approve
  * no live execution enabled

Key thing to watch: **score-batch logs must become visible in Request Log**. That is the last workflow gap before the hub is usable as a full audit loop.
