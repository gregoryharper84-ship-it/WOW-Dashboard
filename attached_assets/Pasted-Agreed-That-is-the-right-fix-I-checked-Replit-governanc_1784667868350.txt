Agreed. That is the right fix.

I checked Replit governance again. The live patch stack already supports your design through:

**`WOW-PATCH-2026-07-15-PROP-CONFIDENCE-AND-MARKET-LABEL-SEPARATION`**

That patch separates:

| Lane               | What it answers                    | Exact market required?       |
| ------------------ | ---------------------------------- | ---------------------------- |
| **Hit confidence** | “Which side is most likely?”       | No                           |
| **Market edge**    | “Is the line mispriced vs market?” | Yes, or capped reconstructed |
| **Money / EV**     | “Does payout create positive EV?”  | Yes                          |
| **Slip approval**  | “Can this be used in a structure?” | Yes, plus correlation/payout |

So the correction is not to remove blockers. The correction is to stop letting one blocker collapse every answer into “not confirmed.”

### New operating rule I’ll use

For props like **Castillo MORE 13.5 1IP**, I’ll return four labels every time:

| Output lane              | Castillo MORE 13.5 example                                                           |
| ------------------------ | ------------------------------------------------------------------------------------ |
| **Confidence lean**      | Can be ranked from reconstructed 1IP model                                           |
| **Market-edge status**   | Exact 1IP market unavailable → `ADJACENT_RECONSTRUCTED` / `MODEL_ONLY_RECONSTRUCTED` |
| **Playable-slip status** | Blocked until PrizePicks payout/slip context is known                                |
| **Final approval**       | Blocked under current 1IP TEST_ONLY / exact-market rules                             |

The uploaded exact-line auditor still correctly prevents fake no-vig pricing, fake PrizePicks per-leg odds, and adjacent-line odds being treated as exact markets. 

### Important terminology fix

`ADJACENT_RECONSTRUCTED` and `MODEL_ONLY_RECONSTRUCTED` should be **evidence/source statuses**, not terminal buckets, unless governance formally adds them as terminal buckets.

So the final label would look like:

**Castillo MORE 13.5**
Confidence lane: **RECONSTRUCTED_LEAN**
Market lane: **MODEL_ONLY_RECONSTRUCTED / exact market unavailable**
Money lane: **PAYOUT_UNRESOLVED**
Slip lane: **NOT SLIP ELIGIBLE**
Terminal bucket: **MODEL_QUALIFIED_HOLD**
can_execute=false

### Practical result

Going forward, web search should fill every public gap before a blocker is assigned. But if no exact market exists, I will not say “blocked and done.” I’ll produce a ranked reconstructed lean while clearly separating it from market-verified edge.

**SKILL COMPLIANCE:** Replit governance active; hash `2a74d11e20ee24526de6db9401f39b4a9228c0f46948d07cea72dc4a21111908`; confidence/market/money/slip separation active; exact-market blocker preserved for money labels; reconstructed lane allowed only as capped research; **can_execute=false**.
