After the currently running B4 task completes, implement and verify a narrowly scoped production repair for two recurring scoring defects. Do not alter B4, settlement work, can_execute=false, governance ceilings, or supported-model policy.

1) Probability output contract: any row returned with MODEL_QUALIFIED_HOLD, MARKET_VERIFIED_HOLD, MONEY_QUALIFIED, FINAL_CONFIDENCE_HIGH, or another probability-bearing qualifying label must expose valid raw_probability, calibrated_probability, lower_bound, upper_bound, model_timestamp, source_snapshot_id, calibration_method, and the required market_no_vig/l10_distribution/role_usage components in the API response. Never manufacture values and never derive raw_probability directly from L5/L10 or market odds. If the registered model cannot produce the complete ledger, candidate_evaluation_completed must remain false and the row must receive a scoped technical failure—not a qualifying label. Preserve completed probability output if a later market/money/slip gate fails.

2) Outlier-review completion: when L5/L10 divergence or OUTLIER_FLAG:REVIEW_REQUIRED fires, the live production prop pipeline must automatically run the governed outlier branch before terminal disposition: identify/isolate outlier games, recompute the required L9/current-role distribution, record excluded event IDs and reasons, update uncertainty/lower bound through the registered sport model, then resume validation. If evidence is insufficient, return a named scoped DATA_CONTRACT_FAIL with missing fields and acquisition attempts. Never leave REVIEW_REQUIRED as the final unresolved state and never bypass the review to qualify a row.

Add focused unit and end-to-end regression tests for WNBA points/assists/rebounds and MLB pitcher strikeouts, including: qualifying label always has a complete probability payload; incomplete probability cannot retain a qualifying label; later market failure preserves valid model output; outlier branch recomputes and completes; insufficient outlier evidence fails closed; mixed batches preserve independent row states. Run the full tests/, gate_engine/tests/, services/tests/ suite and report exact pass/fail/skip counts, changed files, commit, and deployment readiness. Do not deploy automatically.