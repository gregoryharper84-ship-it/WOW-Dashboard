Share these **three items** with Replit:

1. [Backend integration checklist](sandbox:/mnt/data/WOW-v16-Slip-Consistency-Integration-Checklist.md)
   This is the most important file for implementation. It contains the required flags, schema additions, finalizer assertions, and regression tests.

2. [Full consistency patch](sandbox:/mnt/data/WOW-PATCH-2026-07-23-SLIP-CONSISTENCY-AND-FRAGILITY.md)
   This defines PATCH-014 through PATCH-020 and the exact behavioral rules the engine must enforce.

3. [Updated optimizer skill](sandbox:/mnt/data/wow_slip_probability_optimizer_v3/SKILL.md)
   This shows the final workflow and expected outputs after the backend changes are applied.

You do **not** need to give Replit the installable ZIP unless you want it to inspect the packaged skill. That ZIP is mainly for installing the skill into ChatGPT:

[Installable skill ZIP](sandbox:/mnt/data/wow-slip-probability-optimizer-v3-installable.zip)

Paste this instruction to Replit with the files:

> Implement the attached WOW v16 Slip Consistency and Fragility patch in the betting engine. Treat the integration checklist as the coding contract and the patch file as the governance source of truth. Update the slip optimizer, database/schema, finalizer, daily exposure registry, joint-probability logic, settled-slip ledger, and automated tests. Do not only store the markdown files or return a design plan—make the code changes. Preserve all current WOW v16 governance and existing active patches.
>
> Required behavior after implementation:
>
> * default card size is 2, maximum is 3;
> * reject 4–5 pick Power cards;
> * shrink Flex cards above 3;
> * never add filler legs to preserve card size;
> * weakest-leg removal is binding;
> * low-count 0.5/1.5 props use discrete count distributions;
> * high-usage composite LESS props require upper-tail and covariance modeling;
> * TEST_ONLY 1IP props cannot enter final Power or Flex cards;
> * duplicate player/stat/event exposure across active slips is blocked;
> * correlated legs require a joint model;
> * fragile slips cannot publish;
> * settled results must distinguish platform win, full-card hit, gross return, net profit, and positive net return;
> * all regression tests in the checklist must pass;
> * `can_execute=false` and dry-run-only governance remain enforced.
>
> Return:
>
> 1. files changed;
> 2. database migrations or schema changes;
> 3. API changes;
> 4. test results;
> 5. sample output proving the finalizer rejects or shrinks invalid cards;
> 6. confirmation that removed legs cannot be restored downstream.
