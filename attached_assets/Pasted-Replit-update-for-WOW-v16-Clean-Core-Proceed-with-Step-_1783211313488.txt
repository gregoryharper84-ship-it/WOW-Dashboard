Replit update for WOW v16 Clean Core:

Proceed with Step 4 validation for:

WOW-PATCH-2026-07-05-WOW-GPT-RECONCILE

Status right now:
Rewritten, not staged, not deployed. Smoke testing required before any live Custom GPT config update.

Please run validation against the rewritten WOW-BETTING-ENGINE-GPT-INSTRUCTIONS.md only. Do not change backend code in this step unless a mismatch is discovered and explicitly approved later.

Run these smoke tests:

1. SOURCE_CONFLICT visibility
    Input: row has gamelog vs narrative discrepancy >15%, otherwise clean.
    Expected: row remains visible with specific conflict blocker/state. Session NO_PLAY cannot replace the row-level reason.
2. No-market-comp negative test
    Input: strong L10/role data, no direct contradiction, but no market comp and no validated projection-market equivalent.
    Expected: no MONEY_QUALIFIED, no FINAL_APPROVED; cap at MODEL_QUALIFIED_HOLD or backend no-market equivalent. Do not use MARKET_VERIFIED_HOLD without affirmative market/projection support.
3. Generic terminal-word guard
    Expected: terminal row bucket cannot be generic HOLD, WATCH, PASS, LEAN, CONDITIONAL, or NO BET. If WATCH appears, it must be advisory only, not terminal row output.
4. No per-leg Kelly
    Input: one strong leg, but slip-level EV/Kelly unavailable.
    Expected: no per-leg Kelly kill, no FINAL_APPROVED, stake $0 until composite slip-level EV/Kelly exists.
5. Severe-delta bypass guard
    Input: 8%+ market delta but missing status or stale line.
    Expected: candidate may auto-surface for review, but no money label unless status, line, payout, source, market, and live recheck gates pass.
6. PRE_SCORE_EXIT reconciliation
    Input: three rows — one SLATE_PURGE, one DUPLICATE_EXPOSURE_BLOCK, one scored rejection.
    Expected: all three remain countable; pre-score exits are row-countable states, not scoring outcomes; no ROW_COUNT_MISMATCH.
7. Proprietary/no-market guard
    Input: proprietary prop with no direct book market and no calibrated module.
    Expected: max MODEL_QUALIFIED_HOLD or reject, depending on data quality. No FINAL_APPROVED. WNBA triple-risk may appear only as GPT advisory risk, not backend-enforced kill.
8. Conditional cleanup guard
    Expected: the rewritten WOW Betting Engine GPT instructions do not use Conditional as an active label. Keep WOW-PATCH-2026-07-04-CONDITIONAL-CLEANUP open until live Command Center config is verified without Conditional = one layer pending.

Please return:

* final character count of WOW-BETTING-ENGINE-GPT-INSTRUCTIONS.md
* smoke-test results, PASS/FAIL per test
* any changed wording caused by testing
* whether any backend mismatch was found
* final status line

Expected final status if all pass:

WOW-PATCH-2026-07-05-WOW-GPT-RECONCILE — rewritten and smoke-tested; pending Greg/ChatGPT final review before staging/deployment.

Do not paste into the live Custom GPT config yet.