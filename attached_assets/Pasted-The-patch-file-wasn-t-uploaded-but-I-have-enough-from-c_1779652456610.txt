The patch file wasn't uploaded but I have enough from context. The two-line swap is clear: proxy routes are `/rundown/events/<id>/<date>` and `/rundown/sports`, key moves server-side, proxy returns a `fallback_hint` field on 401. Updating the module now.9/9 checks passed. Here's the complete diff from v1 to v2 and the exact deploy steps:

---

**What changed from the v1 patch**

The only structural change is in Tier 1. Instead of calling `therundown.io` directly with `X-TheRundown-Key`, it now calls `${EP.flask}/rundown/events/${trdId}/${dateStr}` with no auth header. The rest of the function — Tier 2 Odds API, Tier 3 ESPN, all normalizers, `assertHasLines` — is identical.

The proxy's `fallback_hint: true` field is the key handshake. When TheRundown returns 401 (plan not yet upgraded), the proxy forwards that signal as a clean JSON body instead of re-throwing a 401 to the browser. The client sees a 200 with `fallback_hint: true`, logs one warning line, and drops straight to Tier 2 without any error noise.

---

**Three things to do in Replit**

**1. Replace the fetch block** — same find-and-replace as v1, just use this file instead.

**2. Remove `KEYS.rundown`** from the browser `KEYS` object. It's fully server-side now. Leaving it in is harmless but misleading.

**3. Confirm `EP.flask` points to your Replit server URL** — the same one your existing Flask routes use. If the variable is named differently (e.g. `EP.replit` or `EP.api`), swap the reference in line 75 of the file.

---

**Verification sequence**

```
Network tab → filter "rundown/events"
  → should show requests to your Flask server (not therundown.io)
  → 200 + { fallback_hint: true }  = proxy live, plan upgrade pending
  → 200 + { events: [...] }        = fully working, lines flowing

Console log
  → "TheRundown MLB: plan limit — trying Odds API"  = fallback_hint path
  → "TheRundown MLB: 15 events ✓"                  = live and working
  → "TheRundown MLB: no games today"               = valid no-slate (not an error)

Regression check
  → /nba-stats/gamelog?player=LeBron James → still 10 games  (untouched)
  → QA badge → must stay green before and after
```

Once the TheRundown plan is upgraded on their side, `fallback_hint` stops appearing and lines start flowing — zero further code changes needed.