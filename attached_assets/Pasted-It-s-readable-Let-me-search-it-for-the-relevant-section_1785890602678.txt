It's readable. Let me search it for the relevant sections instead of reading all 2307 lines.This worked — full file tree saved. Let me grep it for odds/quota/gate_engine paths.Let me fix a formatting slip in that patch (a couple of lines lost their `+` prefix, which would break `git apply`).Found it — lines 208-209 are missing the `+` prefix. Fixing:Now let me create the modified version and generate a real diff with `diff -u` to avoid any more manual line-count errors.This is a real, verified diff (correct hunk headers guaranteed by `diff -u` itself). Now appending it to the main patch file with a proper `diff --git` header line:Patch applies cleanly and produces byte-identical output. Now let me verify the new module and tests actually run correctly with pytest.Now the app.py integration notes — this part can't be a verified diff since the GitHub copy is stale, so I'm flagging that clearly.One flag before the summary: **GitHub `main` is stale.** app.py there has zero trace of `_ODDS_QUOTA_STORE`, `GPT_ACTION_SECRET`, or any `/wow/*` route — none of what `test_odds_quota_tracking.py` (also on that branch) expects. So I couldn't produce a verified diff against the real app.py; everything else is real and tested.

`odds_quota_crossworker.patch` (verified with `git apply --check` + `git apply`, byte-identical round-trip):
- **New** `gate_engine/pg_odds_quota.py` — Postgres write-through/read for cross-worker quota state. `pg_try_advisory_lock(778597324)` (new id, doesn't collide with settlement worker's `778597299` or LLP cron's `778597203`). The lock is best-effort serialization only — the write itself is an atomic `INSERT ... ON CONFLICT DO UPDATE`, so an update is never silently dropped if the lock isn't acquired, which is the actual bug being fixed.
- **New** `gate_engine/tests/test_pg_odds_quota.py` — 6/6 passing locally, no live DB needed (verifies the fail-open contract).
- **Modified** `gunicorn_conf.py` — schedules `ensure_table_exists()` per-worker on `post_fork`, same pattern as the existing Stage 2 bootstrap, plus re-inits `_ODDS_QUOTA_LOCK` alongside the other locks.

`APP_PY_INTEGRATION_NOTES.md` — the exact code to drop into the real app.py (write-through call in `_odds_quota_update`, a new `_odds_quota_snapshot_cross_worker()` used only by the route so the existing empty-store test assertions can't flake), plus everything I couldn't verify locally (full `pytest gate_engine/tests/ -x -q` run, table-name collision check). `can_execute` isn't touched anywhere — confirmed by grep.