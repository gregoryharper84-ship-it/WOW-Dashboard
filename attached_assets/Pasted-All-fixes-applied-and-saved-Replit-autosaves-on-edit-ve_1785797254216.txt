All fixes applied and saved (Replit autosaves on edit — verified 0 TypeScript/lint errors in every touched file). Here's what changed:

**Root cause found:** the "Final Lock" manual research tool (a separate mini frontend app at `artifacts/final-lock/`, plus one backend endpoint) computed its own approval-style label from a number the human typed in, using the exact same string values as the real backend's `terminal_label` vocabulary (`FINAL_APPROVED`, `MODEL_QUALIFIED_HOLD`). That's a textbook authority leak: nothing ran it through the actual gate engine, but it displayed (and in one case, permanently stored in Postgres) text indistinguishable from a real approval.

**Fixed, 3 files:**
- `app.py`, `lock_api_submit` (line ~20279) — the endpoint that inserts into the `final_locks` table now writes `"ESTIMATE_CLEARS_BREAKEVEN"` / `"ESTIMATE_HOLD_BELOW_BREAKEVEN"` instead of `FINAL_APPROVED`/`MODEL_QUALIFIED_HOLD`. This is the persisted DB value — it can no longer collide with a real gate-engine terminal_label.
- `final-lock-dashboard.tsx` — updated the `GateResult` type, the `computeGate()` calculator, and the history table's `approved` check to the same new vocabulary. Added a visible disclaimer under the decision badge: "Local estimate only... not verified by the backend gate engine and not a real terminal_label."
- `prompt.tsx` — same relabel in its inline gate calculator (this one wasn't even rendered as text anywhere, just the type was inconsistent, but fixed for consistency).

I confirmed this didn't break anything: the backend already independently flagged these responses with `"can_approve_bets": false` and a `disclaimer` field, and `leaderboard.tsx`'s use of `MODEL_QUALIFIED_HOLD` is a legitimate filter option against real graded results, not part of this bug — left untouched.

**Everything else from the audit was already sound** (verified with file/line evidence): monotonic ceiling behavior via early-return ordering in `classifier.py`, the Patch #22 mandatory-gate-completion check in `route_registry.py`, fail-closed data-status handling, session-wide + cross-slip exposure governance (`cross_ticket_governor.py` + PATCH-016 in `/build-slips`), and confirmation that `skip_data_contract` isn't exposed over the public HTTP route. The Custom GPT instructions were already rewritten and saved earlier this session.

That closes every gap identified in this audit. Nothing further is pending on my end unless you want me to go build the generic reusable `clampToCeiling()` utility the original spec described — the current code achieves the same monotonic guarantee via strict early-returns per function, just not as one shared helper.