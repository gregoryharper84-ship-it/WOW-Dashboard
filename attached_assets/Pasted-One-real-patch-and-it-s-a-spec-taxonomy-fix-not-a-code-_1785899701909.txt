One real patch, and it's a spec/taxonomy fix, not a code bug (I don't have evidence of broken code — that needs the ledger pull I flagged):

**Add failure code `G4-WORKLOAD`** to the postmortem taxonomy, with a hard rule: any settled MLB Outs-MORE (or pitch-count-MORE) loss defaults to `MODEL ERROR` unless `required_out_survival_lower_bound` was actually reported at pick time. Right now a miss like Jones's has nowhere to go but the catch-all `G4-CONTEXT`, which loses the specific, recurring signal — and without the fail-closed default, a missing field silently gets waved through as "variance" instead of flagging that the gate may not have run.

This affects two things: the `wow-postmortem-auto` skill's Step 5 table, and possibly `gate_engine/mlb_directional_firewall.py` / `failure_path.py` if you want it enforced in code rather than just checked at postmortem time.

Two ways I can take this further — want me to:Major finding — `mlb_directional_firewall.py` is real, current, and I found an actual code gap in it. Let me verify against the file tree and check test coverage before drafting anything.Confirmed: `blockers: []`, silently promoted to `MODEL_QUALIFIED_HOLD` — identical to a row that actually passed the check. Now applying the fix:Now let's verify the fix and write a full regression test suite covering all branches:All three behaviors confirmed correct. Now writing the full pytest-style regression suite (filling the gap that none existed for this module):All 13 pass. Now generating the verified diff and running it through the same `git apply` validation:Cleaner to just regenerate this properly. Let me fix the sandbox file's import back to the real relative import and re-diff from scratch:Clean, minimal diff now. Let me validate it applies from a fresh checkout, then append the new test file and re-verify the combined patch:Now appending the new test file to the patch:Fully verified. Now drafting the formal `WOW-PATCH-TEMPLATE.md`-formatted proposal:Both deliverables are ready.Found a real bug: `_apply_outs_more_gate()` never checked for a missing `required_out_survival_lower_bound` — a None value fell through all three rules and landed on the same `MODEL_QUALIFIED_HOLD` as a row that actually cleared the 0.65 floor. Missing data and passing data were indistinguishable.

Fixed with a Rule 0 that routes missing/non-numeric survival data to `REJECT_DATA_QUALITY` instead. 13 new regression tests (module had zero before), patch applies clean, `can_execute` untouched throughout.

Three files: the patch, a plain-English notes doc, and the formal WOW-PATCH-TEMPLATE doc (labeled `v16 test candidate`, ready for legacy platform Agent — not yet activated, that's your call). It doesn't tell you whether Jones's specific miss was variance or this exact gap — that still needs the ledger row.