Patch it in both places, but Replit is the priority.

Here’s the split:

Patch here first: GPT / WOW spec

Use ChatGPT/WOW instructions for the governance rule:

No Kalshi sports contract may advance beyond LLP_WATCH unless fresh direct Kalshi orderbook data, sportsbook no-vig consensus, final-lock recheck, market status, and edge threshold all pass.

This keeps future analysis from saying “playable” off web-page prices or scan-only odds.

Patch Replit second: actual enforcement

This is where the real fix belongs.

The blocker was not just model judgment. It was missing infrastructure:

Kalshi API orderbook pull
market status / trading_active check
freshness timestamp gate
final-lock rerun window
spread/liquidity tax
WATCH trigger alerts
CLV + settlement ledger logging

So the actual “over the hump” patch has to be in Replit, because GPT alone cannot enforce fresh orderbook, timestamps, or final-lock automation.

My recommendation

Do it in this order:

1. Patch here:
   Add the rule to WOW v16 / LLP Kalshi governance.
2. Patch Replit:
   Add orderbook + final-lock + stale-price enforcement.
3. Add tests:
   Make sure web-price-only can never return LLP_PLAYABLE.
4. Add ledger:
   Track WATCH candidates for CLV, not just approved plays.

Exact Replit patch target

Tell Replit/Claude to patch wherever these live:

/wow/llp/kalshi/ml-evaluate
Kalshi scanner
Kalshi market client
orderbook fetcher
LLP label resolver
final-lock evaluator
settlement / CLV ledger

The most important enforcement rule:

if kalshi_orderbook_source != "direct_api":
    cap_label = "LLP_WATCH"
if orderbook_age_minutes > 10:
    final_label = "KALSHI_DATA_UNOBTAINABLE"
if final_lock_recheck != true:
    cap_label = "LLP_WATCH"
if adjusted_edge < threshold:
    final_label = "LLP_REJECT" or "LLP_WATCH"

Send this to Replit / Claude

Patch request: WOW-PATCH-2026-07-07-KALSHI-FINAL-LOCK-EDGE-DISCOVERY

Implement this in the LLP Kalshi sports lane.

Goal:
Prevent Kalshi sports contracts from advancing beyond LLP_WATCH unless direct executable market data, sportsbook consensus, final-lock recheck, and edge thresholds all pass.

Rules:

1. Web UI Kalshi prices are display-only and cannot satisfy executable price freshness.
2. A contract cannot advance beyond LLP_WATCH without direct Kalshi API orderbook data.
3. Required orderbook fields: ticker, yes_bid, yes_ask or implied buy price, no_bid, no_ask or implied sell price, spread, available size, timestamp, market status, and exchange trading_active.
4. If direct orderbook fetch fails, return KALSHI_DATA_UNOBTAINABLE or cap at LLP_WATCH depending on existing lane taxonomy.
5. If orderbook age is greater than 10 minutes, return KALSHI_DATA_UNOBTAINABLE.
6. If market is not open/trading_active, return KALSHI_REJECT_BAD_RULES or LLP_REJECT.
7. If final-lock recheck has not run inside the configured window, cap at LLP_WATCH.
8. For MLB main winner markets, require adjusted edge of at least 1.5% after price/spread/liquidity/friction tax.
9. For derivative markets such as F5 3-way, require at least 2.5% adjusted edge.
10. Rejected and WATCH candidates must be logged with scan price, final-lock price, no-vig consensus, model probability, label, blocker tags, closing price, settlement result, and CLV.
11. can_execute must remain false under DRY_RUN_ONLY_NO_LIVE_TRADING_NO_MARKET_ORDERS.

Regression tests required:

* web-price-only candidate cannot return LLP_PLAYABLE.
* missing orderbook cannot return LLP_PLAYABLE.
* stale orderbook cannot return LLP_PLAYABLE.
* final-lock-skipped candidate cannot return LLP_PLAYABLE.
* edge below threshold cannot return LLP_PLAYABLE.
* fresh orderbook + market open + final lock + edge above threshold can return LLP_PLAYABLE, but can_execute remains false.

Bottom line: patch the rule here, but build the fix in Replit. GPT should govern the decision. Replit should enforce it.