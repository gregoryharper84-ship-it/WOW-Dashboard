Send this entire message to Replit:

Sync and deploy the completed full-board confidence enforcement fix from GitHub.

Repository:
gregoryharper84-ship-it/WOW-Dashboard

Required branch:
migration/phase4-transfer-20260825

Required commit:
0b885751ad5e16bb9a690cd8d85a74db9b21dc3b

The GitHub implementation and CI are already complete. Do not redesign or replace the fix.

Proceed through the following steps:

1. Inspect the current Replit branch, HEAD, remotes, and worktree status.
2. Preserve any uncommitted or Replit-only work. Do not reset, overwrite, delete, or silently discard changes.
3. Fetch GitHub and fast-forward/sync the Replit workspace to:
    origin/migration/phase4-transfer-20260825
4. Confirm that commit 0b885751ad5e16bb9a690cd8d85a74db9b21dc3b is present in the deployed source lineage.
5. Confirm these files and changes exist:
    * gate_engine/full_board_confidence.py
    * gate_engine/tests/test_full_board_confidence.py
    * Full-board confidence enforcement in gate_engine/daily_orchestrator.py
    * GPT reporting guard in gpt-instructions-daily-orchestration.md
6. Run:
    * The focused full-board confidence tests
    * Daily orchestrator tests
    * The repository’s required-three regression suite
    * Any deployment smoke tests required by the project
7. Do not deploy if a required test fails. Diagnose and report the exact failure first.
8. If all required tests pass, deploy the backend using the existing production deployment configuration.
9. After deployment, verify the live backend—not only the local workspace.

Required production acceptance checks:

A. Incomplete-board case:

discovered_rows=61
confidence_accounted_rows=4
model_eligible_rows=61

Expected:

status=FULL_BOARD_RUN_INCOMPLETE
optimizer_allowed=false
promising_count_claim_allowed=false
can_execute=false

B. Complete-board case:

Every model-eligible row terminates exactly once as one of:

HIGH_CONFIDENCE
MEDIUM_CONFIDENCE
LOW_CONFIDENCE
NO_CONFIDENCE
GLOBAL_BLOCKER

Expected:

status=FULL_BOARD_CONFIDENCE_PASS
confidence_accounted_rows=model_eligible_rows
optimizer_allowed=true
promising_count_claim_allowed=true
reconciliation.reconciled=true
can_execute=false

C. Failure-precedence case:

If an originating lifecycle or scoring failure exists—such as:

DISCOVERY_CHECKPOINT_UNAVAILABLE
FALLBACK_PREFLIGHT_UNAVAILABLE
PRIMARY_SCORER_SCORING_STAGE_TIMEOUT

that specific cause must remain the primary failure reason. FULL_BOARD_RUN_INCOMPLETE may remain as a separate derived blocker but must not overwrite the root cause.

D. Reporting behavior:

The backend or GPT must never claim “only X props are promising” for the complete board unless:

full_board_confidence.status=FULL_BOARD_CONFIDENCE_PASS
promising_count_claim_allowed=true

If the board is incomplete, it may report a clearly labeled partial research subset, but it must not present the subset count as a full-board conclusion.

Return the following evidence after deployment:

* Replit branch
* Synced workspace HEAD
* Production deployed commit/version
* Test commands and exact pass/fail totals
* Deployment identifier and completion timestamp
* Live health/status response
* Results of acceptance checks A–D
* Confirmation that can_execute=false
* Any remaining blocker or unverified item

Do not report “deployed,” “complete,” or “production verified” until the live deployed service has passed these checks.