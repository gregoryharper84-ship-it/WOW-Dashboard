Replit does **not** need the whole spec rebuilt. It needs only the **pending dashboard/automation items** that are separate from the analytical spec.

## Replit needs 4 things

### 1. **Component B — pybaseball MLB automation patch**

This is the new item created by the MLB source-stack upgrade.

Replit needs a separate patch to automate:

* MLB L5/L10/L20 ledger pulls
* Statcast form-window pulls
* FanGraphs/Baseball Reference-derived baseline pulls
* Pitcher-vs-opponent K simulations
* CSV/export output for the WOW dashboard
* failure handling when pybaseball fails or Replit is down

Important: pybaseball should be treated as **Tier 1 equivalent only when the source pull succeeds**, never Tier 0, because it is derived from other sources rather than official MLB.com.

### 2. **Existing pending Replit patches**

Your master spec still has these pending:

* `REPLIT-PROVENANCE-LABEL`
* `REPLIT-INPUT-NORMALIZATION`
* `REPLIT-BBREF-FETCH-FAILURE-FLAG`

Deployment order should stay:

1. `REPLIT-PROVENANCE-LABEL`
2. `REPLIT-INPUT-NORMALIZATION`
3. `REPLIT-BBREF-FETCH-FAILURE-FLAG`

### 3. **New failure/status fields**

Replit should support the new MLB source workflow fields:

* `weather_checked`
* `weather_source`
* `weather_risk`
* `weather-park-not-checked`
* `mlb_starter_confirmed`
* `starter_confirmation_source`
* `lineup_confirmed`
* `batting_order_slot`
* `statcast_checked`
* `fangraphs_checked`
* `market_sanity_checked`

These should appear in the candidate output or validation report so we know whether a prop is capped, watch-only, or eligible to advance.

### 4. **Smoke-test requirements**

Before Replit is considered “updated,” it needs tests for:

* pitcher prop with unconfirmed starter more than 2h before first pitch → **WATCH only**
* pitcher prop unconfirmed inside 2h → **hard kill**
* hitter TB/HRR without weather check → add `weather-park-not-checked`, cap label
* pitcher K without Savant/FanGraphs → cannot pass skill-quality gate
* pybaseball failure → fallback to manual Tier 1 ledger, do not fake data
* stale team/no game today → slate purge kill

## What to send Replit

Send this:

```text
Replit needs Component B only.

Do not modify WOW analytical rules. The active spec is already merged as WOW v16 Clean Core.

Implement a separate pybaseball MLB automation patch that supports:
1. MLB L5/L10/L20 ledger pulls
2. Statcast form-window pulls
3. FanGraphs/Baseball Reference-derived baseline pulls
4. pitcher-vs-opponent K simulations
5. CSV/export output for WOW dashboard
6. clear source provenance labels
7. failure handling and fallback when pybaseball/API pulls fail

Also support new MLB workflow fields:
- weather_checked
- weather_source
- weather_risk
- weather-park-not-checked
- mlb_starter_confirmed
- starter_confirmation_source
- lineup_confirmed
- batting_order_slot
- statcast_checked
- fangraphs_checked
- market_sanity_checked

Keep deployment order:
1. REPLIT-PROVENANCE-LABEL
2. REPLIT-INPUT-NORMALIZATION
3. REPLIT-BBREF-FETCH-FAILURE-FLAG
4. New pybaseball Component B patch

Run wow-pr-checker before merge and wow-smoke-test after deploy.

Do not treat pybaseball as Tier 0. If pybaseball succeeds, output is Tier 1 equivalent because it is derived from Baseball Reference, Baseball Savant, and FanGraphs. If it fails, mark source as Failed/Not Available and fall back to manual Tier 1 ledger process. Do not fake validation.
```

Bottom line: **Replit needs the automation patch, not the spec patch.** The spec is already updated.
