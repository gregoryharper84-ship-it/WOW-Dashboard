# Disposable scratch file created during FIX-2026-09-20-001 root-cause
# reproduction. Not part of the fix; the sandboxed worker's `rm`/`mv` tools
# were unavailable to delete it. Safe to delete; do not commit it.
