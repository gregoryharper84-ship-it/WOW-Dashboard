# gate_engine/fantasy_score_model/generators/__init__.py
# Sport-specific Fantasy Score generators.
# Each module exposes generate_one(params, rng) -> float.

can_execute: bool = False  # unconditional
