# ============================================================================
# RETIRED — DO NOT INSTALL
# ============================================================================
#
# This schema has been retired as of 2026-08-14.
#
# The four Odds API operations it contained have been migrated into the
# canonical Gate Engine GPT Action schema (gpt-action-schema-gate-engine.yaml)
# under the unified X-API-Key / ApiKeyAuth contract.
#
# Auth change:
#   BEFORE: X-WOW-Action-Key (GPT_ACTION_SECRET)
#   AFTER:  X-API-Key (SCORING_API_KEY) — same as all other scored routes
#
# Why this file is kept:
#   - Preserves a record of the original schema for rollback reference.
#   - Prevents accidental re-creation of a duplicate-domain Action.
#
# If you see this file referenced in a GPT Action editor, remove that Action
# and use the Gate Engine action (gpt-action-schema-gate-engine.yaml) instead.
#
# The server routes /wow/odds/events, /wow/odds/event-markets,
# /wow/odds/event-odds, and /wow/odds/quota-status now reject X-WOW-Action-Key
# and require X-API-Key.  Requests sent with X-WOW-Action-Key alone will
# receive HTTP 401.
# ============================================================================

openapi: 3.1.0
info:
  title: "WOW Odds Gateway [RETIRED — use gpt-action-schema-gate-engine.yaml]"
  version: "0.0.0-retired"
  description: >
    RETIRED. This schema is no longer installable or valid.
    The four Odds API operations have been merged into the Gate Engine schema.
    Do not install this Action. See gpt-action-schema-gate-engine.yaml.
  x-retired: true
  x-retired-date: "2026-08-14"
  x-replacement: "gpt-action-schema-gate-engine.yaml"

# No servers, paths, or security schemes — intentionally empty so this
# schema cannot be installed as a functioning GPT Action.
