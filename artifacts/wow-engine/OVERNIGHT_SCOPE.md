# Overnight Scope

Tonight's engineering scope is to close code/configuration/runtime blockers that can be proven safely. It explicitly excludes force-promoting outcome-dependent calibration gates and excludes any live wager execution path.
